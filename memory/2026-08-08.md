# 2026-08-08 — Chiquinho Sorvetes demo

## Site profissional (pedido do usuário)
- Logo oficial atualizada (2025) baixada do site da franquia:
  - https://chiquinho.com.br/wp-content/uploads/2025/04/chiquinho-logo-horizontal.png (600x107)
  - https://chiquinho.com.br/wp-content/uploads/2025/04/chiquinho-logo-vertical.png (578x578)
  - Convertidas de PNG paleta+tRNS para RGBA limpo com `sips -s format png` (o original em paleta causa bug de pintura no Chrome)
  - Arquivos: `assets/logo-horizontal.png`, `assets/logo-vertical.png`, `assets/favicon.png`, `assets/icon-192.png`
- `index.html` reescrito (profissional, mobile-first): logo no header azul, badges (telefone/horário), card de chat com cabeçalho de marca, botões "Falar com a equipe" (WhatsApp wa.me/5521990323063) e "Ligar agora" (tel:+5521990323063), FAB de WhatsApp com pulso, rodapé azul escuro, favicon, meta tags.
- Nome da loja ficou no chat-head (não no header) — ver bug de pintura abaixo.
- `server.py` agora serve `/assets/*` (GET estático). Servidor roda com `nohup python3 server.py` na porta 8788 (0.0.0.0).

## Acessos
- Celular (Tailscale): http://100.95.115.103:8788 — testado funcionando (chat responde preços reais)
- Mesmo Wi-Fi: http://192.168.1.113:8788
- Local: http://127.0.0.1:8788
- Config aplicada: `browser.ssrfPolicy.allowedHostnames = ["127.0.0.1", "100.95.115.103"]` (após aprovação do usuário) — navegador do OpenClaw agora abre a demo.

## LIÇÃO GRANDE: bug de pintura do Chrome 151 (headless E real)
- Sintoma: header (gradiente azul + logo `<img>` + badges) e rodapé deixam de PINTAR (fundo some, fica a cor do body) quando certos conteúdos existem na página.
- Gatilhos confirmados: **qualquer bloco de texto avulso** (ex.: cartão welcome com h1+p entre header e main, ou um div/h1 adicionado no topo do main) + badges+logo no header → header não pinta. Imagem carregada + textos no mesmo elemento do header também quebra.
- Estrutura VERIFICADA que funciona: header = só `img` + badges; nome/tagline fora do header; rodapé só texto (pinta no Chrome real, NÃO pinta no headless — artefato headless-only).
- Headless (`--headless=new --screenshot --disable-gpu --virtual-time-budget`) é NÃO-CONFIÁVEL para verificar esta página: mente sobre header/rodapé. **Sempre verificar no navegador real (browser tool: navigate + screenshot)**.
- ~30 iterações de bisseção (testbed local com python http.server na 8799 + decoder PNG próprio). F4/F5 "funcionavam" por CSS inválido acidental — cuidado com sed que quebra regras CSS.

## Ferramentas de verificação sem visão
- Modelo de visão indisponível (opencode/gpt-5-nano sem pagamento) — usei decoder PNG em Python puro (atenção: bpp correto por color type 2→3 bytes, 6→4; aplicar unfilter PNG) e Gemini CLI (`gemini -p` com imagem, chave no keychain "gemini-cli-api-key") para descrever screenshots.
- O gemini CLI tem chave configurada; usar para análise visual quando necessário.

## Tela inicial estilo Linktree (pedido do usuário ~21:31)
- `index.html` reescrito com 2 telas trocadas por hash (`#/` home, `#/chat` chat):
  - **Home estilo Linktree**: logo + badges no header, card de boas-vindas e 4 botões grandes: 📍 Mapa (Google Maps busca "Chiquinho Sorvetes Teresópolis" — endereço exato continua desconhecido), 🍦 Cardápio da loja (abre o chat e envia "Quero ver o cardápio"), 💬 Chat com o atendente, 🛵 iFood (busca no ifood.com.br — link exato da loja ainda não temos).
  - **Botão "Ligar agora" REMOVIDO** (e todo link `tel:`). Botão WhatsApp mantido dentro do chat + FAB.
  - Botão voltar (←) no chat-head volta pra home.
- `server.py`: respostas determinísticas novas (antes do LLM):
  - `MENU_RE` + `build_menu_summary()`: pedido genérico de cardápio → resumo das linhas com preço mínimo real (R$ 4,00, excluindo Adicional/EMBALAGEM). Ignora se a msg contém nome de grupo (com normalização de acentos via unicodedata — ex. "açaí" vs grupo "Açai") para não roubar perguntas de produto específico.
  - `ADDRESS_RE`: pergunta de endereço/localização → aponta para o botão Mapa da página inicial (sem inventar endereço).
- Verificado no navegador real (browser tool + act:evaluate): header pintou OK (sem bug de pintura), 4 botões, sem botão de ligar, Cardápio→chat envia mensagem e bot responde com resumo, voltar funciona. Linktree: sem `tel:`.
- iFood: link direto da loja fornecido pelo Mateus — https://www.ifood.com.br/delivery/teresopolis-rj/chiquinho-sorvetes---teresopolis-varzea-varzea/6b48b54f-4318-4d3d-a749-024e05d186d6 (commit 0c54d04). Atenção: ele primeiro mandou um link de São Paulo/Higienópolis (99ac2891-...) que troquei pelo de Teresópolis assim que ele corrigiu — conferir sempre se o link é da loja de Teresópolis.
- Cuidado: `pgrep -f "python3 server.py"` NÃO mata o processo (cmdline é `Python server.py`). Usar `lsof -i :8788 -sTCP:LISTEN` para achar o PID.
- Commit: 4560feb.

## GitHub Pages (deploy definitivo ~22:10) 🚀
- Mateus preferiu GitHub Pages em vez de Render (estático, grátis, sem servidor).
- **Mudança grande:** `index.html` agora tem o atendente 100% client-side (JS puro, sem fetch /chat): carrega `menu_index.json` e replica as regras determinísticas do server.py (horário, endereço→botão Endereço, resumo de cardápio, busca com normalização de acentos, indisponíveis, fallback honesto). Nunca inventa preço.
- Caminhos trocados p/ relativos (`assets/...`, `menu_index.json`) — Pages serve em subdiretório; caminhos absolutos `/assets` quebrariam.
- Removidos: `render.yaml`, `Dockerfile`, `.dockerignore` (obsoletos). `server.py` ficou só como servidor LOCAL de dev (ganhou rota GET /menu_index.json). `.gitignore` agora: `.env` + `.DS_Store`.
- Busca não inclui grupos Adicional/EMBALAGEM (evita ruído tipo "Ad Açaí R$ 4" nos resultados).
- Repo: **https://github.com/ChipNorth17/chiquinho-sorvetes** (público, conta criada via gh device flow — código 080B-3522, login OK ~21:55). Site: **https://chipnorth17.github.io/chiquinho-sorvetes/** — build 200, menu 792 itens carregando, botões OK, bot responde (verificado no navegador real).
- `gh` CLI instalado via brew; `gh repo create --public --source=. --push`; Pages via `gh api repos/.../pages -X POST`.
- LIÇÃO: a outra sessão (chat principal) também mexeu no site (commit c587b00: avatar circular, OG tags, botão renomeado p/ "Endereço", horário 21:00 no SYSTEM — que o Mateus corrigiu p/ 21:30 depois). Sempre reler index.html antes de editar.

## Git
- Repositório git inicializado mas nunca commitado; criei `.gitignore` com `.env` (contém chave da API) e commitei o resto.

## Contatos do projeto
- WhatsApp da equipe: (21) 99032-3063 (número informado pelo usuário; pedir confirmação à loja)
- WhatsApp do dono (Mateus): (21) 99454-2037 (chatbot responde que confirma pedidos por lá)
- Horário da loja: todos os dias 10:30–21:30 (CONFIRMADO pelo Mateus em 08/08 ~22:08 — ele pediu pra mudar de 21:00 pra 21:30). Determinístico no bot.
- Endereço da loja: NÃO encontrado/não inventar.

## Melhorias Linktree (21:54)
- Botão "Mapa" renomeado para "Endereço" (pedido do Mateus: endereço, iFood, chat, cardápio).
- Avatar circular (logo-vertical.png) no card de boas-vindas — visual Linktree clássico.
- OG tags (og:title/description/type) para preview bonito ao compartilhar o link.
- Histórico: houve inconsistência de horário no server.py (SYSTEM dizia 21:30, site 21:00). Depois o Mateus confirmou que fecha 21:30 → agora tudo 21:30 (commit posterior).
- Verificado no navegador real: 4 botões OK, 0 links tel:, avatar carregou. Server reiniciado na 8788.
